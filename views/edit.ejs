<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit your message</title>
  </head>
  <body>
    <h3>edit your chat for id : <%= chat.id %></h3>
    <p>sent by : <%= chat.from %></p>
    <form action="/chats/<%= chat.id%>?_method=PUT" method="post">
      <h4>edit your message</h4>
      <textarea name="msg" cols="30" rows="10">
        <%= chat.msg %></textarea> 
        <br>
        <button>edit</button>
    </form>
  </body>
</html>
